A 94% HDI is the shortest interval that contains 94% of the probability mass of a posterior distribution
Gelman-Rubin statistic R -> check if chains are exploring the same distribution


y_init is p(0)


 What we plot: All 4 cells — cell 0 (baseline) + cells 1-3 (post-injection). That's the full cleaned EEG, all 342 trials for S01.       
                                                                                                                                         
  Why the t=20min plasma point is missing: The last plasma sample for S01 is at 20 min post-injection. On our sequential time scale, that
   maps to 20 + 4.25 = 24.25 min — but the EEG data only spans 17.1 min of sequential time (342 trials × 3s). So the last plasma dot     
  falls off the right edge of the plot.                                                                                                  
                                                                  
  Why the EEG is shorter than 20 min: The raw recording spans ~27.5 min of wall-clock time, but a lot of that is gaps — artifact-rejected
   segments, the injection gap (~5 min), and inter-trial gaps. Only the clean 3-second epochs survive in data_trials3s.mat. Those 342
  clean trials = 17.1 min of sequential time, even though the actual experiment lasted ~25 min post-baseline. 



  -exclusion of both subject 3 (flat LZ experience) and subject 9 (missing data). From this
point onwards, subject 11 will also be excluded from all the experiments outlined beyond
this section


The observed plasma data for Pi was intentionally excluded
due to complications on several aspects: its difficulty of cross-subject normalization, data
sparsity challenges and undesirable convergence experiences (see Appendix B for details).
Therefore, the design of the Bayesian model graph for a pooled two compartment model is
shown in figure 18. Given the Bayesian inference consider each parameter being probabilistic in nature, parameters k0, k1, k2 and y init are all random variables. y init represents
P(0) which is the initial DMT concentration in plasma at time = 0. Each of the model
parameter has a prior distribution defined in the graph node. For example, k0, k1, k2
∼ HalfCauchy .
Moreover, the variables y1 and y2 are derived through a deterministic computational
process involving two intermediary variables, alpha and beta which in turn are contingent
upon the rate parameters. Note that alpha and beta are merely middle layer variables which
have no biological interpretations. Instead they are essential components of the analytical
solution to the previous ODEs 13 (see section 5.2 for details).
Last, the plasma concentrations, represented as variable plasma and LZc values, represented as variable lz, are modelled to follow normally distributed priors as plasma ∼
N (y1, plasma sigma) lz ∼ N (y2, lz sigma). Finally, the model fitting process exclusively
leverages variable lz as the observed variable, which is shown in the grey box


Trace is the output of the MCMC sampling

python -m src.paper_model.run_pipeline --observe-plasma --draws 1000 --chains 2
Available flags:
  - --observe-plasma — fit both LZc + plasma
  - --plasma-only — fit plasma only (no LZc)
  - Neither — fit LZc only (original behavior)
  - --load-trace
    --only-after-injection


--------------------------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------------------------


- solo ha utilizado lzc para fittear,

b(t) is stimated with lzc, so all units are lzc units. we can not predict mg/ml

- esta fiteando el modelo con un gap de tiempo en el medio (creo que eso esta mal) (he probado a fittearlo tbm post injection)

- to compartment es uno para el cerebro y otro para la sangre, pero claramente en la sangre solo va a decrecer, tbm por como se ve en la diff eccuation


- I assume the dmt data starts collecting at injection time (nosotros solo tenemos 5 y en el otro paper 9)

- No podemos utilizar este modelo a no ser que queramos predecir LZC

- El modelo que te enseñe la semana pasada podría ser una alternativa (sino el paper que me diste aunque ahi todos los del mismo grupo a la vez y no esta nada especificado los detalles)

- are we trying to predict DMT concentration on brain or on blood? supongo que blood porque brain no la sabemos


- Experimento para diferenciar entre pre injection y post injection. Pille 5 mins antes y despues de la injección.
  -SimpleCNN -> funciona
  -Graphormer -> funciona
    - Node features = spectral band power (5 values per node)
      -  Power Spectral Density (PSD), integrate over range, log
    - Edge features = amplitude envelope correlation (1 value per edge)
      - Hilbert transform, extract amplitude envelope, pearson correlation
    - SPD is one






#

fitear usando la concentracion en plasma y tissue response hill equation. solo post injection
  - e_max es 1, E= lz, A=y2, EC y n se fittean



$$\frac{E}{E_{\max}} = \frac{[A]^n}{\text{EC}_{50}^n + [A]^n}$$ 


drug concentration in the brain doesn't translate 1:1 into a brain complexity change.Hill equation is a dose-response function that maps a drug 
  concentration to an effect.



-------------------

EEG data was then collected from one minute prior to drug administration and placebo up to 20 minutes after = False

Blood samples of 10 ml were collected in EDTA tubes prior to and at approximately 2, 5, 12, 20, 33 and 60 minutes after DMT administration. =>
=> asumo que el sample en t=0 es pre-injection, asumo también que ese t=0 se corresponde aprox con el grupo después del parón (linea verde de los plots) (realmente pienso que sería un pelin antes)


Spectral and spontaneous signal diversity analysis were performed for 5 minutes-averaged data before and immediately after DMT/Placebo administration (time-averaged results). Additionally, we performed analysis using 1 minute averages throughout the whole 20 minutes of EEG recordings following DMT/Placebo injection (time-sensitive results). =>
=> esto confirma un poco mi teoria

si comparo los plots tiene sentido


- i fit he model as you told me
- calculate lz values only post injection without normalizing, i made sure they were between [0 and 1]

- lognormal distribution means the log of the variable is normally distributed
EC50 = pm.LogNormal("EC50", mu=np.log(100.0), sigma=1.0)
n_hill = pm.LogNormal("n_hill", mu=0, sigma=0.5)

- EC50 is the brain DMT concentration (in ng/mL) at which LZc reaches 50% of its maximum. 
  -mu=log(100) centers the prior on 100 ng/mL
  -sigma=1.0 gives a 95% prior interval of roughly [14, 710] ng/mL, wide enough to be learned from data
  -we use lognormal to avoid it degenerating to creo

-The Hill coefficient controls the steepness of the dose-response curve.
  - mu=0 centers prior on 1
  - sigma=0.5 gives a 95% prior interval of [0.4, 2.7]

- y_init_sigma ~ Exponential(lam=0.002) (mean 500)
- si no se usa este init la escala no cuadra   

plasma_sigma = pm.HalfCauchy("plasma_sigma", beta=50.0)  # prior 95% interval is roughly [0, 636] ng/mL, median 50 ng/ml




- R2 suggest no learning, but MAE decreases
- The wider the range the bigger the mae































- surrogate lz calculation approach:
Surrogate normalization (Casali et al. 2013) takes a different approach: shuffle the binary sequence randomly to destroy all temporal structure, then compute LZ76 on that shuffled version. The shuffled sequence has the maximum possible complexity for a sequence with that same proportion of 0s and 1s. Dividing by this surrogate complexity gives a ratio in [0, 1] that measures "how complex is this signal relative to a random signal with the same statistics."